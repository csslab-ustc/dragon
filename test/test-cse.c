int foo(int x, int y, int z) {
    int a, b, c, d, e;

    a = x + z;
    if (x > 0) {
        b = x - y;
        b = x - y;
        b = x - y;
    } else {
        c = x - y;
        c = x - y;
        c = x - y;
        z = 2;
    }
    d = x - y;
    e = x + z;
    return 0;
}